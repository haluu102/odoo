run code: python odoo-bin -r dbuser -w dbpassword --addons-path="addons,custom" -d mydb

dbuser is the PostgreSQL login, dbpassword is the PostgreSQL password, and mydb is the name of the PostgreSQL database
